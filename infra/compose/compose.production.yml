services:
  api:
    environment:
      APP_ENV: production
    deploy:
      resources:
        limits:
          memory: 1g
  worker:
    environment:
      APP_ENV: production
    deploy:
      resources:
        limits:
          memory: 1536m
  customer-agent:
    environment:
      APP_ENV: production
    deploy:
      resources:
        limits:
          memory: 768m
